version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_PATH=/app/data/keystone.db
      - PROOF_SECRET=keystone-dev-secret-change-in-production
      - DEFAULT_ORG_ID=org_demo
      - DEFAULT_API_KEY=ks_test_demo_key_001
    volumes:
      - keystone-data:/app/data

  ui:
    build: ./ui
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000
      - NEXT_PUBLIC_API_KEY=ks_test_demo_key_001
    depends_on:
      - backend

volumes:
  keystone-data: